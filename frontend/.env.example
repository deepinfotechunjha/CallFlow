# Example environment variables for local development
# Copy this to ".env" and adjust values as needed.

# Frontend API URL - set this to your deployed backend URL in production
VITE_API_URL=http://localhost:4000
